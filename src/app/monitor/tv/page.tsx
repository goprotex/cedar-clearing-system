import MonitorClient from '../MonitorClient';

export const metadata = {
  title: 'Live Monitor TV — Cedar Hack',
};

export default function MonitorTvPage() {
  return <MonitorClient fullscreen />;
}

